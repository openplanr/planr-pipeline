# Error Report — failed task handoff

> **Generated by:** [Frontend Agent | Backend Agent | QA Agent] (per dispatch)
> **Generated at:** [ISO-8601 timestamp]
>
> **Canonical output paths (basename):** `tasks/T-<TASK_ID>-error-report.md`
>
> - Default mode example: `output/feats/feat-{name}/us-{N}/tasks/T-004-error-report.md`
> - Spec-driven example: `<SPEC_DIR>/tasks/T-004-error-report.md`

`<TASK_ID>` **must** equal the YAML `id` (`^T-\d{3}$`) field of the task file that exhausted **R6**.

**Correction-loop semantics:** do **not** paraphrase here — follow **`docs/rules.md`** → **`### R6 — Max 3 Correction Iterations`** verbatim for pass order, forbidden shortcuts, and when to STOP.

---

## Task Summary

```yaml
Feature: feat-{name}   # or SPEC slug in spec-driven prose
UserStory: us-{N} — [US title]
Task id: T-NNN
Task file: …/tasks/T-NNN-{slug}.md …   # prose path pointing at authoring artifact
Type: [UI | Tech]
Agent: [Frontend Agent | Backend Agent]
Stack: [language/framework/orm pulled from stack.md]
```

---

## Attempt log

Document each correction **pass** (up to the R6 cap) with: trigger, failing command from `stack.md`, truncated output, code change summary, outcome. See **R6** for the required shape of passes 1–3.

---

## Final State

### Files Created
- [list every file actually created during correction passes, even partial ones]

### Files Modified
- [list with brief description of changes — exclude reverts]

### Files Preserved (Verified Untouched)
- [list — confirm these were not modified]

### Outstanding Failures
- [the specific compile errors, test failures, or DoD checks that still fail]

---

## Suspected Root Cause

[2–4 sentences. Be specific. Examples:]
- "Task spec references `users.email_verified` column that does not exist in `output/db/schema.json`. Likely the spec was written against a newer schema. Re-run DB Agent or update spec."
- "Component library `shadcn/ui` is named in stack.md but `npm list` shows it isn't installed. Run `npx shadcn-ui@latest init` and re-attempt task."
- "Test framework `Jest` cannot resolve TypeScript path alias `@/features/*`. `tsconfig.json` paths and `jest.config.js` moduleNameMapper are out of sync."

---

## Recommended Human Action

Pick one and elaborate:

- [ ] **Fix the spec.** Edit `us-{N}.md` or the parent spec, then re-run `/planr-pipeline:plan {name}` to regenerate tasks.
- [ ] **Fix the environment.** Install missing dependency / add env var / update stack.md, then `/planr-pipeline:ship {name} --task T-NNN` to retry narrowly.
- [ ] **Fix the schema.** Re-run DB Agent (`/planr-pipeline:plan {name} --rescan`) and retry the stalled task flag.
- [ ] **Override.** This task spec is unrealistic for the current codebase; revise scope manually.

---

## Reproduction

To reproduce locally:

```bash
# from project root
[exact BuildCommand]
[exact TestCommand]
```

Last attempt's working tree state: [git rev-parse HEAD if in git, else "no VCS"].

---

*Consumed by snapshot / Known Issues rollups and by **QA Agent** when surfacing a failed gate.*
