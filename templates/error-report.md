# Error Report — task-{M} of us-{N} of feat-{name}

> **Generated by:** [Frontend Agent | Backend Agent | QA Agent] (Opus 4.7 / Sonnet 4.6)
> **Generated at:** [ISO-8601 timestamp]
> **Lives at (default mode):** `output/feats/feat-{name}/us-{N}/tasks/error-report.md`
> **Lives at (planr spec-driven mode):** `.planr/specs/SPEC-NNN-{slug}/tasks/error-report.md`
> **Why this exists:** Per `docs/rules.md` R6, an agent that fails 3 correction iterations MUST stop and write this report. This file is the handoff to a human reviewer.

---

## Task Summary

```yaml
Feature: feat-{name}
UserStory: us-{N} — [US title]
Task: task-{M}.md — [task title]
Type: [UI | Tech]
Agent: [Frontend Agent | Backend Agent]
Stack: [language/framework/orm pulled from stack.md]
```

---

## Iteration Log

### Iteration 1 — Direct Fix
- **Trigger:** [build error | test failure | preserve-list violation | other]
- **Failure command:** `[the exact BuildCommand or TestCommand from stack.md]`
- **Failure output (first 50 lines):**
  ```
  [paste truncated build/test output]
  ```
- **Action taken:** [what the agent changed]
- **Outcome:** failed (carried into iteration 2)

### Iteration 2 — Holistic Re-read
- **Re-loaded:** [task-{M}.md | design-spec.md | schema.json | stack.md]
- **Hypothesis:** [why iteration 1 failed]
- **Action taken:** [what changed]
- **Outcome:** failed (carried into iteration 3)

### Iteration 3 — Minimal Safe Fix
- **Action taken:** [the smallest change that could plausibly compile/pass]
- **Outcome:** failed → STOP

---

## Final State

### Files Created
- [list every file actually created during the 3 iterations, even partial ones]

### Files Modified
- [list with brief description of changes — exclude reverts]

### Files Preserved (Verified Untouched)
- [list — confirm these were not modified]

### Outstanding Failures
- [the specific compile errors, test failures, or DoD checks that still fail]

---

## Suspected Root Cause

[2–4 sentences. Be specific. Examples:]
- "Task spec references `users.email_verified` column that does not exist in `output/db/schema.json`. Likely the spec was written against a newer schema. Re-run DB Agent or update spec."
- "Component library `shadcn/ui` is named in stack.md but `npm list` shows it isn't installed. Run `npx shadcn-ui@latest init` and re-attempt task."
- "Test framework `Jest` cannot resolve TypeScript path alias `@/features/*`. `tsconfig.json` paths and `jest.config.js` moduleNameMapper are out of sync."

---

## Recommended Human Action

Pick one and elaborate:

- [ ] **Fix the spec.** Edit `us-{N}.md` or the parent spec, then re-run `/openplanr-pipeline:plan {name}` to regenerate tasks.
- [ ] **Fix the environment.** Install missing dependency / add env var / update stack.md, then re-run only this task.
- [ ] **Fix the schema.** Re-run DB Agent (`/openplanr-pipeline:plan {name} --rescan`) and re-run this task.
- [ ] **Override.** This task spec is unrealistic for the current codebase; revise scope manually.

---

## Reproduction

To reproduce locally:

```bash
# from project root
[exact BuildCommand]
[exact TestCommand]
```

Last attempt's working tree state: [git rev-parse HEAD if in git, else "no VCS"].

---

*This file is consumed by `/snapshot` (rolled into the "Known Issues / Escalations" section of `CLAUDE.md`) and by the QA Agent (which surfaces it as a failed gate).*
