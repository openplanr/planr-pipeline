{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "test -f .claude/.snapshot-pending && rm -f .claude/.snapshot-pending && echo '[planr-pipeline] /ship aborted before snapshot — re-run /planr-pipeline:ship to refresh CLAUDE.md' || true"
          }
        ]
      }
    ]
  }
}
