{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "test -f .claude/.snapshot-pending && rm -f .claude/.snapshot-pending && echo '[openplanr-pipeline] /ship aborted before snapshot — re-run /openplanr-pipeline:ship to refresh CLAUDE.md' || true"
          }
        ]
      }
    ]
  }
}
