// Preserve anchor (lock-listed barrel). Never modified by T-001.
// NOTE: src/feature.ts does NOT exist in this main tree — the declared write was
// WITHHELD because the worktree also produced the undeclared src/undeclared.ts.
// This barrel intentionally does not re-export ./feature: the feature module
// never landed.
export {};
