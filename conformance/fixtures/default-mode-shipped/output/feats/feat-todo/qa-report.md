# QA report — conformance default-mode fixture (gold)

Synthetic pass record for **`verify-ship --dir conformance/fixtures/default-mode`**.

- **gates:** BuildCommand (`npx tsc --noEmit`) and TestCommand (`npx vitest run`) exercised in-folder.
- **marker:** `.pipeline-shipped` present beside this file.
